<div class="section">
    <div class="columns is-centered">
        <div class="column is-4">
            <h1 class="title is-3">Your events</h1>
            
        <% @user.own_events.each do |event| %>
            <p><%= link_to event.theme, event_path(event) %></p>
        <% end %>

        <%= link_to "Back", root_path %>
        </div>
    </div>
</div>