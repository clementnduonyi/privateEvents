<div class="column is-one-quarter">
    <nav class="panel">
        <p class="panel-heading">Attendees</p>
        <% User.find(@event.attendances.attending.map {|attended| attended.attendee_id}).each do |attendance| %>
                <p class="panel-block"><%= attendance.name %></p>
        <% end %>
    </nav>
</div>